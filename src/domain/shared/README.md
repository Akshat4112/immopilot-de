# Shared financial primitives

Cent-exact money, high-precision decimal rates, PD-007 round-half-up boundaries and stable domain
validation errors. Import supported utilities from `index.ts`; calculators must not reach into
individual implementation files across domain-module boundaries.

See `docs/calculation-engine-conventions.md` for the normative contract.
