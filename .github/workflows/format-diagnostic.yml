name: Format diagnostic

on:
  push:
    branches:
      - cf-003-financing-required-equity

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Print formatter diff
        run: |
          npx prettier src/domain/financing src/domain/README.md --write
          git diff --no-color
